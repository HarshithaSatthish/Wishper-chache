You are a Senior AI Research Engineer + Startup CTO.

Your task is NOT to generate sample code or partial outputs.

Your task is to build a FULLY WORKING, END-TO-END, PRODUCTION-READY system called EVT-CLIP.

This system MUST be:

* Fully functional (no placeholders, no mock data)
* Runnable locally without errors
* Capable of real inference (image → heatmap)
* Structured cleanly for production deployment

If anything is incomplete → YOU MUST FIX IT.
If anything is assumed → YOU MUST IMPLEMENT IT.

---

## 🚨 CORE OBJECTIVE

Build EVT-CLIP: a Zero-Shot Anomaly Segmentation system using CLIP + DAEP + CMI.

INPUT:

* Industrial image

OUTPUT:

* Pixel-level anomaly heatmap
* Anomaly score (0–1)

This MUST work in real execution.

---

## ⚙️ REQUIRED SYSTEM (END-TO-END)

You MUST build ALL of the following:

1. MODEL CORE (CRITICAL)

   * Load OpenCLIP ViT-L-14-336
   * Extract patch features AND attention weights
   * Implement attention hooks if needed

   IMPLEMENT:
   ✅ DAEP Module:

   * Attention rollout (Abnar & Zuidema)
   * CLS → patch attention map
   * Top-K patch selection
   * Cross-attention with prompt tokens

   ✅ CMI Module:

   * Self-attention over patches
   * Cross-attention with text embeddings
   * Cosine similarity (good vs damaged)
   * Anomaly map generation
   * Upsampling to original resolution
   * Gaussian smoothing using kornia

   🚫 DO NOT SKIP ANY STEP
   🚫 DO NOT SIMPLIFY ARCHITECTURE

   Every tensor shape MUST be correct and verified.

---

2. REAL INFERENCE PIPELINE

---

Build complete pipeline:

image → preprocess → CLIP → DAEP → CMI → heatmap → overlay

Requirements:

* torch.no_grad()
* mixed precision (autocast)
* GPU support (CUDA)
* fallback to CPU if GPU unavailable

Output:

* anomaly_score (float)
* heatmap (numpy array)
* overlay image (OpenCV)

---

3. FASTAPI BACKEND (PRODUCTION)

---

Build FULL backend:

Endpoints:

POST /api/v1/analyze

* Accept image
* Run model
* Return:
  {
  anomaly_score,
  heatmap_base64,
  processing_time_ms,
  patch_scores,
  threshold
  }

GET /api/v1/health
GET /api/v1/samples

Requirements:

* Model loads ONCE (singleton)
* Async endpoints
* Threadpool inference
* Error handling
* Logging
* CORS enabled
* Rate limiting

🚫 No fake responses allowed

---

4. REACT FRONTEND (WORKING UI)

---

Build full UI:

* Image upload
* Show preview
* Call API
* Display:

  * Heatmap overlay
  * Score
  * Toggle modes

Tech:

* React + TypeScript + Tailwind
* Axios API calls
* Canvas overlay

🚫 Must actually connect to backend
🚫 No static UI

---

5. EVALUATION PIPELINE

---

Build script:

* Run on MVTec dataset
* Compute:

  * AUROC
  * PRO
  * AP
  * F1-max

Output:

* CSV
* Markdown table
* Sample heatmaps

If dataset not present:
→ gracefully skip and log warning

---

6. DOCKER + DEPLOYMENT

---

Generate:

* Dockerfile (GPU support)
* docker-compose.yml
* .env.example

System must run with:

docker-compose up --build

---

7. PROJECT STRUCTURE

---

Generate COMPLETE structure:

backend/
app/
model.py
daep.py
cmi.py
routes.py
schemas.py
utils.py
main.py

frontend/
src/
components/
pages/

Also include:

* requirements.txt (pinned)
* package.json
* README.md
* .gitignore

---

8. VALIDATION (MANDATORY)

---

Before finishing, you MUST:

1. Run a test inference (dummy image)

2. Verify:

   * No crashes
   * Heatmap generated
   * API responds correctly

3. Simulate full flow:
   upload → backend → result

If ANY step fails:
→ Fix automatically

---

9. PERFORMANCE TARGET

---

* Inference time: < 3 seconds (GPU)
* No memory leaks
* No NaN values
* Stable outputs

---

## 🚨 HARD RULES

* NO placeholders
* NO fake outputs
* NO “TODO”
* NO skipping difficult parts

If something is complex:
→ break it down and implement step-by-step

---

## 🎯 FINAL OUTPUT FORMAT

You MUST return:

1. Complete folder structure
2. All backend code files
3. All frontend code files
4. Setup instructions
5. Run commands
6. Example API request + response
7. Verification proof (how to test it)

---

## 🧠 EXECUTION MODE

Think like:

* A researcher (correctness)
* A production engineer (stability)
* A startup CTO (speed)

Build it FULLY.

Do not stop until everything works.

---

## END OF TASK
