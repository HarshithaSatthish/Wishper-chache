{
  "name": "@whispercache/circuits",
  "version": "1.0.0",
  "description": "Zero-Knowledge circuits for WhisperCache memory verification",
  "main": "index.js",
  "scripts": {
    "compile:hash": "circom hashVerifier.circom --r1cs --wasm --sym -o build",
    "compile:memory": "circom memoryVerifier.circom --r1cs --wasm --sym -o build",
    "setup:hash": "snarkjs groth16 setup build/hashVerifier.r1cs pot12_final.ptau build/hashVerifier.zkey",
    "setup:memory": "snarkjs groth16 setup build/memoryVerifier.r1cs pot12_final.ptau build/memoryVerifier.zkey",
    "export:vkey": "snarkjs zkey export verificationkey build/hashVerifier.zkey build/verification_key.json",
    "test": "node scripts/test-circuit.js"
  },
  "keywords": [
    "zk",
    "snark",
    "circom",
    "privacy"
  ],
  "author": "WhisperCache Team",
  "license": "MIT",
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.5"
  }
}
