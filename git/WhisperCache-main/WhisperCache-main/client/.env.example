# WhisperCache Client Environment Variables (Example)

# API Configuration
VITE_API_URL=http://localhost:4000

# Feature Flags
VITE_ENABLE_DEBUG=false
VITE_ENABLE_MOCK_MODE=false

# Storage Mode: 'memory' | 'session' | 'persistent'
VITE_KEY_STORAGE_MODE=session
